# CopyCat-Finder
An AI-powered desktop app to find and clean up duplicate photos using CLIP embeddings and Flet.

To get started, create a venv in VS code.

python -m venv venv

Activate the venv.

venv\Scripts\activate

Then to install all libraries and dependencies:

pip install -r requirements.txt.
